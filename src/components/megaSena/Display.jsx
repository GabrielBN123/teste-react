import React from 'react'

export default (props) => {
    return(
        <div>
            <h4 className="displayMega">Número: <span className="numSpan">{props.numMega}</span> </h4>
        </div>
    )
}